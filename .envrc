# direnv configuration for Nix flake
# Automatically load the Nix development environment
use flake

# Load all runtime variables from .env (single source of truth).
# The dotenv command reads KEY=VALUE lines and exports them.
dotenv .env

# Load deploy variables if present
dotenv_if_exists infra/deploy.env
